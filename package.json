{
  "name": "campos-munos-redesign",
  "private": true,
  "description": "Monorepo root. Frontend lives in site/, backend in server/. The root scripts exist so Railway's Railpack builder detects this as a Node project. The postinstall hook fires automatically from Railpack's default `npm install` and installs both subdirs.",
  "scripts": {
    "postinstall": "cd site && npm ci && cd ../server && npm ci --omit=dev",
    "build": "cd site && npm run build",
    "start": "cd server && npm start"
  }
}
